Reverse String

"""
Instructions:
    reverse_string() accepts a string (chars) and should return all the
    characters in reverse order. 

    Examples:
        reverse_string("hello") = "olleh"
        reverse_string("Recursion Worksheet") = "teehskroW noisruceR"

"""

def reverse_string(chars: str) -> str:
    
    return None